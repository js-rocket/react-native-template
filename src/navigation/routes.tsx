// The enumeration or all the routes.  Prevents dev accidentally typing in wrong route name

enum routes {
  Intro,
  Login,
  Help,
  // Routes in tabs begin here
  TabsHome,
  Home,
  Gallery,
  Settings,
}

export default routes
